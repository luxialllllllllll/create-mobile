{
  "name": "create-ex-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Mobile web version of create-ex for Render deployment.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check app.js"
  },
  "engines": {
    "node": ">=20"
  }
}
